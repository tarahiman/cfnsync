// Temporary scaffold placeholder (T-01).
//
// `src/{cli,usecase,core,ports,aws,backend,report}` are intentionally empty
// at this stage (see docs/spec/tasks.md T-02 onward) -- the TypeScript
// compiler requires at least one input file to build successfully, so this
// file exists only to make `npm run build` meaningful until real modules
// land. Replace/remove it once `src/cli` gets its real entry point (T-19).
export {};
