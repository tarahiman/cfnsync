import { describe, expect, it } from 'vitest';

// Placeholder test to confirm the T-01 scaffold (vitest + tsconfig + directory
// layout) is wired up correctly. Delete this once T-02 (core/config) adds the
// first real test suite.
describe('scaffold', () => {
  it('runs under vitest', () => {
    expect(1 + 1).toBe(2);
  });
});
